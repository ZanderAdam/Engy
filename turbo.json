{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "persistent": true,
      "cache": false,
      "passThroughEnv": ["ENGY_DIR", "PORT", "ENGY_SERVER_URL", "ENGY_VOICE_DUMP"]
    },
    "start": {
      "persistent": true,
      "cache": false,
      "passThroughEnv": ["ENGY_DIR", "PORT", "ENGY_SERVER_URL", "ENGY_VOICE_DUMP"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", "dist-server/**"]
    },
    "lint:eslint": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
